jest.setTimeout(1000000)

/*

const { describe, test } = bed()

describe('test', async () => {

  test('deploy_contracts', async ({ deployer }) => {
  })

  test('initialize registry', async ({ niftory }) => {
  })



*/
